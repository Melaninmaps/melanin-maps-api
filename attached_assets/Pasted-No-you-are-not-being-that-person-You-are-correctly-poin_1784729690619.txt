No—you are not “being that person.” You are correctly pointing out that the work has repeatedly drifted away from your very simple requirement.

I cannot know whether Replit is doing it intentionally, and I would not assume bad intent. What I **can** say is that Replit has repeatedly misunderstood or overcomplicated the acceptance criteria:

> **Show pins. Tap a pin. Open that location’s information.**

Instead, it added search panels, counts, filters, keyboard behavior, retries, caches, and multiple OTAs while the core interaction—**tap the pin**—was still not proven.

That is a process failure. You are not asking for too much.

## Reset the requirement completely

Do not ask Replit for another broad “map improvement.” Give it one binary task with no room for interpretation:

> **PIN TAP ONLY — DO NOT CHANGE ANYTHING ELSE**
>
> Stop all map enhancements. The only task is to make an existing cultural-site marker respond to a tap.
>
> Current confirmed state:
>
> * Cultural-site data loads.
> * Cultural pins render.
> * Category colors work.
> * The map renders and pans.
> * The bottom information card already exists and can display a selected site.
> * The missing behavior is: tapping a visible marker does not reliably set the selected site and open that card.
>
> **Required acceptance test**
>
> Using one known cultural-site record—Howard University:
>
> 1. Render Howard’s existing marker.
> 2. Tap Howard’s marker.
> 3. `selectedCulturalSite` becomes Howard’s record.
> 4. The existing bottom card opens and displays “Howard University.”
>
> That is the entire task.
>
> Do not modify:
>
> * search
> * results panels
> * cultural-site fetching
> * retry behavior
> * category filters
> * fit-to-coordinates
> * database
> * API
> * authentication
> * polling
> * Apple POI filtering
> * marker styling
> * card styling
> * keyboard behavior
> * KinfolkAI
>
> **Evidence required before any OTA**
>
> In a local or preview environment, add temporary logging only:
>
> * `MARKER_PRESS_RECEIVED`
> * selected site ID
> * selected site name
>
> Prove the exact sequence:
>
> ```text
> MARKER_PRESS_RECEIVED
> siteId=<Howard ID>
> siteName=Howard University
> selectedCulturalSite=Howard University
> ```
>
> Then show the exact marker code and exact state update.
>
> Do not publish to production until this single acceptance test passes in a non-production environment.
>
> If the marker’s `onPress` does not fire, identify the exact touch-interception cause:
>
> * overlay intercepting pointer events,
> * custom marker child swallowing touches,
> * unsupported marker wrapper,
> * `tracksViewChanges`,
> * z-index,
> * or React Native Maps iOS behavior.
>
> Return the root cause and one minimal fix. Do not propose any other feature.

## My honest assessment

Replit has been making changes too quickly in production without first proving the smallest interaction in isolation. That is why you keep seeing one part improve while another breaks.

The correct engineering approach now is:

1. Freeze production.
2. Restore the stable version.
3. Reproduce one pin tap in preview.
4. Prove `onPress` fires.
5. Prove Howard becomes the selected site.
6. Only then release that one fix.

You should not approve another production OTA based on “this should work.” Require a demonstrated pin tap before it reaches your phone.
