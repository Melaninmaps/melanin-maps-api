Here is the full final audit — every finding from this session, classified and actionable:

# Mapping With Melanin™ — Community Beta 2 Launch Checklist
**Audit Date:** July 19, 2026  
**Scope:** Apple-priority authentication beta — pre-Railway deployment  
**Authorization phrase required to implement any slide/design change:** "Please implement."
---
## PART 1 — ENGINEERING COMPLETE (all fixed this session)
All items below were discovered by live simulation and patched in this session.
All fixes confirmed live via curl against the running API server.
| # | Issue | Fix Applied | Verified |
|---|-------|-------------|----------|
| 1 | `POST /api/cultural-sites/stories/pending` unprotected — no auth check | Added `isAdmin` middleware | ✅ Returns 403 |
| 2 | `PATCH /api/cultural-sites/stories/:id/moderate` unprotected | Added `isAdmin` middleware | ✅ Returns 403 |
| 3 | `POST /api/referrals/track` unprotected — no auth check | Added `requireAuth` + 24h dedup | ✅ Returns 401 |
| 4 | `/debug/crash-report` and `/download/mobile-build.zip` public routes existed | Routes deleted from routes/index.ts | ✅ Both return 404 |
| 5 | `community_posts` dedup query used `WHERE user_id` — column is `author_id` | Fixed raw SQL column name | ✅ Posts create and dedup 409 correctly |
| 6 | `family_add_on_seats` query used `seats`/`user_id` — schema has `seat_count`/`owner_id` | Fixed both column names | ✅ `/api/membership/plan` returns tier/tierDisplay |
| 7 | Community posts had no double-tap guard | Added 30-second dedup window before insert | ✅ Second identical post returns 409 |
| 8 | Settings screen showed hardcoded version string | Replaced with `Constants.expoConfig?.version` | ✅ Dynamic — tracks app.json |
---
## PART 2 — LIVE API SIMULATION RESULTS (23 endpoints tested)
### Authentication & Registration
| Endpoint | Result |
|----------|--------|
| `POST /api/auth/register` | ✅ OK — requires: firstName, lastName, email, password, username, agreeToTerms:true |
| `POST /api/auth/login-email` | ✅ OK — returns Bearer token |
| `GET /api/auth/user` (Bearer) | ✅ OK — returns full user profile |
| `GET /api/auth/user` (no auth) | ✅ OK — returns {user:null} (correct) |
| `POST /api/auth/forgot-password` | ✅ OK — returns {success:true} |
| `POST /api/auth/phone/send-otp` | ✅ OK — Twilio Verify call confirmed |
### Business Discovery
| Endpoint | Result |
|----------|--------|
| `GET /api/businesses` | ✅ 106 businesses returned |
| `GET /api/businesses?search=BBQ` | ✅ Filtered — 3 results (correct param is `search`, not `q`) |
| `GET /api/membership/stats` | ✅ {foundingMemberCount:0, spotsRemaining:500} |
### Member Actions (authenticated)
| Endpoint | Result |
|----------|--------|
| `POST /api/saved-places` | ✅ {saved:true} |
| `GET /api/saved-places` | ✅ Returns savedBusinessIds array |
| `POST /api/saved-places` (duplicate) | ✅ Silently deduped (onConflictDoNothing) |
| `POST /api/community/posts` | ✅ FIXED — post creates, returns id |
| `POST /api/community/posts` (duplicate) | ✅ FIXED — 409 "Duplicate post detected" |
| `GET /api/membership/plan` | ✅ FIXED — returns {tier:"free", tierDisplay:"Explorer"} |
| `POST /api/surveys` | ✅ Route confirmed — 24h new-account wait (anti-spam, expected) |
| `GET /api/kinfolk/preferences` | ✅ Returns full preferences object |
### KinfolkAI
| Endpoint | Result |
|----------|--------|
| `POST /api/kinfolk/chat` (no auth) | ✅ Works — free-tier monthly limit enforced (intentional product design) |
| `POST /api/kinfolk/chat` (authenticated) | ✅ Works — BBQ recommendations returned |
### Security Enforcement (all unprotected routes now fixed)
| Endpoint | Result |
|----------|--------|
| `POST /api/referrals/track` (no auth) | ✅ 401 "Authentication required" |
| `GET /api/cultural-sites/stories/pending` (no auth) | ✅ 403 "Admin access required" |
| `PATCH /api/cultural-sites/stories/:id/moderate` (no auth) | ✅ 403 "Admin access required" |
| `GET /api/healthz` | ✅ {"status":"ok"} |
---
## PART 3 — CONFIGURATION AUDIT (Verified / Missing / Founder Action)
### Replit Environment Variables
| Variable | Status | Notes |
|----------|--------|-------|
| DATABASE_URL | ✅ VERIFIED | Replit Postgres connected, 106 businesses confirm live data |
| SESSION_SECRET | ✅ VERIFIED | Set in Replit secrets |
| RESEND_API_KEY | ✅ VERIFIED | Set — email sequences active |
| TWILIO_ACCOUNT_SID | ✅ VERIFIED | Set — OTP send confirmed live |
| TWILIO_AUTH_TOKEN | ✅ VERIFIED | Set |
| TWILIO_VERIFY_SERVICE_SID | ✅ VERIFIED | Set |
| GOOGLE_MAPS_API_KEY | ✅ VERIFIED | Set — map embeds active |
| REVENUECAT_API_KEY | ✅ VERIFIED | Set |
| REVENUECAT_API_KEY_V2 | ✅ VERIFIED | Set |
| DOCUSIGN_RSA_PRIVATE_KEY | ✅ VERIFIED | Set |
| DOCUSIGN_USER_ID | ✅ VERIFIED | Set |
| DEFAULT_OBJECT_STORAGE_BUCKET_ID | ✅ VERIFIED | Set |
| ADMIN_EMAILS | ✅ VERIFIED | Set |
| WMATA_API_KEY | ✅ VERIFIED | Set |
### EAS Build Environment (eas.json production profile)
| Item | Status | Notes |
|------|--------|-------|
| EXPO_PUBLIC_REVENUECAT_IOS_API_KEY | ✅ VERIFIED | `appl_evRvvCBBMxNrhJJKrqQELZrxeJv` — publishable, correct in eas.json |
| EXPO_PUBLIC_REVENUECAT_ANDROID_API_KEY | ✅ VERIFIED | `goog_YtlteWfYyxtiWoOvOylWcxKaIBk` — publishable, correct |
| GOOGLE_MAPS_API_KEY (in eas.json) | ✅ VERIFIED | Client-side key, restrict by bundle ID in Google Cloud Console |
| autoIncrement | ✅ VERIFIED | true — EAS will auto-increment before next build |
| Current iOS buildNumber | ✅ VERIFIED | 84 (next build → 85) |
| Current Android versionCode | ✅ VERIFIED | 60 (next build → 61) |
| Current version | ✅ VERIFIED | 1.1.5 |
| iOS bundleIdentifier | ✅ VERIFIED | com.melaninmaps.app |
| Android package | ✅ VERIFIED | com.melaninmaps.app |
### Build Credentials
| Item | Status | Notes |
|------|--------|-------|
| dist_cert.p12 | ✅ VERIFIED | Present in artifacts/mobile/ |
| dist_profile.mobileprovision | ✅ VERIFIED | Present |
| credentials.json | ✅ VERIFIED | Present |
| google-service-account.json | ✅ VERIFIED | Present at artifacts/mobile/google-service-account.json |
### Railway Production Environment (⚠️ CANNOT VERIFY FROM REPLIT — FOUNDER MUST CONFIRM)
| Variable | Status | Action Required |
|----------|--------|-----------------|
| NODE_ENV | ⚠️ UNVERIFIED | Must be set to `production` in Railway dashboard |
| DATABASE_URL | ⚠️ UNVERIFIED | Must point to Railway Postgres (NOT Replit's helium host) |
| STRIPE_SECRET_KEY | ⚠️ UNVERIFIED | Must be set — Stripe payments inoperable without it |
| STRIPE_WEBHOOK_SECRET | ⚠️ UNVERIFIED | Must match the Stripe Dashboard webhook signing secret |
| REVENUECAT_WEBHOOK_AUTH_KEY | ⚠️ UNVERIFIED | Must match RevenueCat dashboard webhook auth header |
| CRON_SECRET | ⚠️ UNVERIFIED | Required for `/api/cron/*` scheduled job endpoints |
| FRONTEND_URL | ⚠️ UNVERIFIED | Set to `https://mappingwithmelanin.com` for CORS and email links |
| COMPANY_MAILING_ADDRESS | ⚠️ UNVERIFIED | Required for email footers (CAN-SPAM compliance) |
| SESSION_SECRET | ⚠️ UNVERIFIED | Must match Replit value exactly |
| RESEND_API_KEY | ⚠️ UNVERIFIED | Required for transactional emails in production |
| TWILIO_* (3 vars) | ⚠️ UNVERIFIED | Required for phone OTP |
| GOOGLE_MAPS_API_KEY | ⚠️ UNVERIFIED | Required for map embeds |
| ADMIN_EMAILS | ⚠️ UNVERIFIED | Must include your email — or call `POST /api/admin/bootstrap` after first login |
### External Service Configuration (Founder Dashboard Actions)
| Item | Status | Action |
|------|--------|--------|
| Railway DB schema sync | ⚠️ REQUIRED | Run `pnpm --filter @workspace/db run push` with `DATABASE_URL` pointing at Railway Postgres before first deploy |
| Stripe webhook endpoint | ⚠️ REQUIRED | Point to `https://api.mappingwithmelanin.com/api/billing/webhook` (or Railway URL) |
| RevenueCat webhook endpoint | ⚠️ REQUIRED | Point to `https://api.mappingwithmelanin.com/api/billing/rc-webhook` |
| Apple TestFlight invites | ⚠️ REQUIRED | Internal testers must be invited in App Store Connect before they can install |
| GoDaddy CNAME unlock | ⚠️ REQUIRED | www.mappingwithmelanin.com is locked by GoDaddy website builder — disconnect before Railway CNAME takes effect (call 1-480-505-8877 or disconnect via GoDaddy dashboard → Website) |
| Google Cloud: restrict Maps API key | 🟡 RECOMMENDED | Add `com.melaninmaps.app` as bundle ID restriction in Google Cloud Console |
| Apple: review App Privacy Nutrition Label | 🟡 RECOMMENDED | Ensure location, health, purchase disclosures match actual data collected |
---
## PART 4 — DEFINITIVE STATUS SUMMARY
| Category | Status |
|----------|--------|
| **Engineering** | ✅ COMPLETE — all 8 code issues patched and verified live |
| **Security** | ✅ COMPLETE — all previously unprotected admin/write routes now enforce auth |
| **API Simulation** | ✅ COMPLETE — 23/23 endpoints tested; all critical user journeys confirmed |
| **Replit Configuration** | ✅ COMPLETE — all 14 env vars set |
| **EAS Build Config** | ✅ COMPLETE — RC keys, credentials, build numbers all confirmed |
| **Railway Configuration** | ⚠️ FOUNDER ACTION REQUIRED — 13 env vars to verify in Railway dashboard |
| **External Services** | ⚠️ FOUNDER ACTION REQUIRED — DB push, Stripe webhook, RC webhook, GoDaddy |
| **Apple Distribution** | ⚠️ FOUNDER ACTION REQUIRED — TestFlight invites, App Store Connect |
---
## PART 5 — PRE-LAUNCH SEQUENCE (in order)
**Step 1 — Railway environment (before deploying)**
- Verify all 13 Railway env vars in Railway dashboard
- Confirm DATABASE_URL points to Railway Postgres internal URL
**Step 2 — DB schema push (one-time, before first traffic)**
```bash
DATABASE_URL="<railway-postgres-url>" pnpm --filter @workspace/db run push

Step 3 — Deploy to Railway

Push latest code → Railway auto-deploys
Confirm GET /api/healthz → {"status":"ok"} on production URL
Step 4 — Admin bootstrap (first login only)

Log in with your account on the production app
Call POST /api/admin/bootstrap to promote yourself to admin
Then set ADMIN_EMAILS=your@email.com in Railway dashboard and redeploy
Step 5 — External service webhooks

Update Stripe webhook URL to production endpoint
Update RevenueCat webhook URL to production endpoint
Verify both return 200 on test event from each dashboard
Step 6 — DNS

Unlock GoDaddy CNAME for www.mappingwithmelanin.com
Point to Railway domain
Step 7 — EAS build (from artifacts/mobile/)

cd artifacts/mobile && eas build --platform ios --profile production

Next build will be: Community Beta 2 (iOS Build 85 / Android Version 1.1.5 Build 61)

Step 8 — TestFlight

Invite internal testers in App Store Connect
Notify: "Everyone should now be testing Community Beta 2 (iOS Build 85)"
Engineering complete as of July 19, 2026. All Railway and external service items require founder action in external dashboards — none are blockers that engineering can resolve without dashboard access.