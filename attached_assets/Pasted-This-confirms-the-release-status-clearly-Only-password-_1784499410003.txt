This confirms the release status clearly:

**Only password reset is fully verified end to end.** The admin fix is complete in code but not live, and the mobile auth fixes still need Builds 90/64 before they can be tested.

The safest next move is to use the same GitHub browser authorization flow that worked previously, rather than sharing a personal access token or refreshing Railway solely for deployment.

Send Replit:

> The authentication matrix is accepted.
>
> Use GitHub browser/device authorization to deploy through the established repository:
>
> ```bash
> gh auth login --web
> ```
>
> I will approve the browser code. Do not request a personal access token.
>
> After authorization:
>
> 1. Build the current API server and web artifacts from commit `a9fbdf65`.
> 2. Confirm the built artifacts include:
>
>    * centralized `adminAuth` helper;
>    * normalized `ADMIN_EMAILS`;
>    * `/admin` returnTo behavior;
>    * already-authenticated redirect;
>    * all prior Railway fixes from commit `020156fd`;
>    * rolling-session server changes already approved.
> 3. Copy only the required updated artifacts into `Melaninmaps/melanin-maps-api`.
> 4. Show `git status` and `git diff --stat`.
> 5. Confirm no secrets, credentials, environment files, database exports or unrelated changes are included.
> 6. Commit and push to `main`.
> 7. Wait for Railway deployment success.
> 8. Report:
>
>    * GitHub commit hash;
>    * Railway deployment ID;
>    * deployment status;
>    * health check;
>    * admin capability-check result.
>
> Then pause for founder admin verification. Do not trigger iOS 90 or Android 64 until I confirm `/admin` works on production.

After the deploy, your verification flow should be:

1. Open `www.mappingwithmelanin.com/admin` in a private browser window.
2. Confirm it redirects to `/login?returnTo=/admin`.
3. Use **Forgot password** for your canonical administrator email if needed.
4. Set the password on that existing account.
5. Log in.
6. Confirm you return automatically to `/admin`.
7. Refresh the page.
8. Confirm the dashboard still loads.
9. Log out and confirm access is removed.

Once that passes, the release can move to:

**ADMIN VERIFIED → trigger iOS 90 / Android 64 → founder retest of the remaining auth rows.**

One correction to Replit’s matrix: phone session persistence is not purely “mobile.” The visible behavior requires the new build, but it also depends on the rolling-session server fix being live. The final release record should list it as **server + mobile**.
