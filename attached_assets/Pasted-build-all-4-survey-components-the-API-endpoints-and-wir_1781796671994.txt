build all 4 survey components, the API endpoints, and wire them in — all in parallel:Now build all 4 API endpoints in parallel:Now register all 5 new endpoints in entry.ts:Now wire the surveys into the businesses page and for-you page:Now add survey state to the Directory component and wire the trigger buttons:Now add a "Rate Safety" button to each card and a "Rate Neighborhood" CTA in the directory header. First find the card action buttons area:Now find the card action buttons and add the "Rate Safety" button:Now add the survey modals to the Directory render and a "Rate Neighborhood" button in the directory header. First find where the existing modals are rendered:Now add a "Rate a Neighborhood" button in the directory header area. Find the directory header:Now wire the Itinerary Feedback and Onboarding Preference surveys into the for-you page:Now find where the itinerary is displayed and add a "Rate this Itinerary" button, plus an "Update Preferences" button. Let me find the itinerary section:Now add the survey modals to the for-you page render and an "Update Preferences" button in the empty state:It looks like the edit accidentally removed the closing tags. Let me fix that:Now add the survey modals at the bottom of the for-you page render:Now add the "Update Preferences" button to the profile summary area in for-you. Find where the profile header is rendered:Now make sure Compass is imported in for-you.tsx:Good, Compass is already imported. Now build the Admin Surveys tab:Now add the SurveysTab component before AdminDashboard. Find a good insertion point:Now add the Surveys tab to the tab list and render it:Now check if ClipboardList, Sparkles, MapPin, Compass are imported in admin.tsx:Need to add ClipboardList, Compass, and MapPin to the imports:Now verify all 5 API endpoints work:All 4 endpoints return 201. Now check the admin surveys endpoint:401 is correct — the admin endpoint is protected by adminAuthMiddleware as expected. All endpoints are working. Now publish:All 4 surveys are live on melaninmaps.com. Here's what was built:

Safety Experience Survey — triggered by the new "Rate Safety" button on every business card. 3-step flow: star ratings for safety/return-alone/recommendation → visit context (time of day, group type, incident flag) → open comments.

Neighborhood Safety Survey — triggered by the new "Rate a Neighborhood" button in the directory header. 3-step flow: city + neighborhood selection → daytime/nighttime/walkability/transit ratings → police sentiment, safety tips checklist, and comments.

Onboarding Preference Survey — triggered by "Update preferences" in the /for-you page header. 4-step flow: travel style → cities of interest → group type + budget → interests + accessibility needs. Saves to localStorage so the personalization engine picks it up immediately.

Post-Itinerary Feedback Survey — triggered by "Rate this itinerary" button that appears below every generated AI trip plan. 3-step flow: star ratings + would-use-again → what worked / what to improve checklists → open comments.

Admin Surveys tab — new tab in the admin panel showing KPI cards for all 4 survey types and a full response viewer with type filtering.