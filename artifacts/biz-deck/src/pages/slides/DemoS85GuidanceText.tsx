export default function DemoS85GuidanceText() {
  const points = [
    { head: "4-tier audience ratings on all content", body: "Every post, event, and library article carries an audience rating — Everyone, Teen, Young Adult, or Adult — set by the author and enforced platform-wide for consistency across content types." },
    { head: "Family Mode — one toggle, full coverage", body: "Parents activate Family Mode in settings to filter all feed, event, and article content to Everyone-rated only. The filter applies across every tab instantly without per-item management." },
    { head: "Family settings for granular control", body: "Members can set separate guidance thresholds for posts, events, and library content — allowing a Young Adult content level for articles while keeping Everyone-only for the community feed." },
    { head: "Badge-first visibility", body: "Audience rating badges render on every piece of content before it's opened — so members see the rating in the card, not after tapping in. No surprises. No retroactive consent." },
  ];

  return (
    <div className="relative w-screen h-screen overflow-hidden" style={{ background: "#1C0E06" }}>
      <div className="absolute inset-0" style={{ background: "radial-gradient(ellipse at 85% 40%, rgba(202,146,43,0.07), transparent 55%)" }} />
      <div style={{ position: "absolute", top: 0, left: 0, right: 0, height: "3px", background: "#CA922B" }} />

      <div className="absolute flex flex-col justify-center" style={{ left: "8vw", right: "8vw", top: "12%", bottom: "12%" }}>
        <div className="font-body" style={{ fontSize: "0.72vw", color: "#CA922B", letterSpacing: "0.24em", fontWeight: 700, marginBottom: "2vw" }}>COMMUNITY JOURNEY · COMMUNITY GUIDANCE + FAMILY MODE</div>
        <div className="font-display" style={{ fontSize: "5vw", fontWeight: 800, color: "#FAF6EF", lineHeight: 1.0, marginBottom: "1.5vw", textShadow: "0 0 40px rgba(202,146,43,0.15)" }}>
          Safe for<br />every household.
        </div>
        <div style={{ width: "5vw", height: "2px", background: "#CA922B", marginBottom: "3vw", opacity: 0.8 }} />
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "2.2vw 4vw" }}>
          {points.map((p, i) => (
            <div key={i} style={{ display: "flex", gap: "1vw", alignItems: "flex-start" }}>
              <div style={{ width: "1.8vw", height: "1.8vw", borderRadius: "50%", background: "rgba(202,146,43,0.15)", border: "1px solid rgba(202,146,43,0.4)", display: "flex", alignItems: "center", justifyContent: "center", flexShrink: 0, marginTop: "0.1vw" }}>
                <span className="font-body" style={{ color: "#CA922B", fontSize: "0.65vw", fontWeight: 800 }}>{i + 1}</span>
              </div>
              <div>
                <div className="font-display" style={{ color: "#FAF6EF", fontSize: "1.05vw", fontWeight: 700, marginBottom: "0.35vw" }}>{p.head}</div>
                <div className="font-body" style={{ color: "#7B5408", fontSize: "0.9vw", lineHeight: 1.65 }}>{p.body}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
      <div style={{ position: "absolute", bottom: "3.5vh", right: "4vw", color: "rgba(202,146,43,0.3)", fontSize: "0.65vw", fontWeight: 700, letterSpacing: "0.12em" }}>85 / 88</div>
    </div>
  );
}
